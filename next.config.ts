/**
 * Template Header
 * Purpose: Next.js build/runtime configuration for the app.
 * Feature Unit: Shared
 * Customize: Add Next.js options (redirects, headers, images, env, etc.) to the
 *   nextConfig object.
 * Depends on: the Next.js framework.
 */

import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
